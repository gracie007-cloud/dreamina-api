name = "dreamina-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# 环境变量（可选，用于设置默认 token）
# [vars]
# DEFAULT_TOKEN = "your-default-token"

# 如果需要使用 KV 存储（可选）
# [[kv_namespaces]]
# binding = "CACHE"
# id = "your-kv-namespace-id"
